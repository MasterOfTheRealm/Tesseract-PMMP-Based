version: 2

requirements:
  signed_off_by:
    required: false
    
group_defaults:
   approve_by_comment:
      approve_regex: ^Approved
      enabled: true
      reject_regex: ^Rejected
   reset_on_push:
      enabled: false
    
groups:

  Owner:
    required: 1
    users:
    - jrodr10
    teams:
    - owners
      
  Software:
      required: 1
      users:
      - jrodr10
      - AppleDevelops
      teams:
      - software
